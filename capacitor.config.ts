import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'android-capacitor',
  webDir: 'dist/android-capacitor',
  bundledWebRuntime: false,
};

export default config;
